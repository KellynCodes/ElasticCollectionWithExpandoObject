using ElasticCollection.Utility;

Application.Start();
/*foreach (var product in ProductDb.ProductSpec)
{
    dynamic Products = new ExpandoObject();
    Products.Name = product.Name;
    Products.Category = product.Category;
    Console.WriteLine($"ID: {Products.Name}, Category: {Products.Category}");
*/
